#include <stdio.h>

#define p *ptr += 1;
#define m *ptr -= 1;

#define f ptr += 1;
#define b (ptr -= 1);

#define o putchar(*ptr);
#define i *ptr = getchar();

#define s while (*ptr > 0) {
#define e }

#define brain int main(){ char* array = new char[3000]; for (int x = 0; x < 2999; x++) { array[x] = 'a';} char* ptr = new char; ptr = array;
#define heck }

brain p p p p p p p o f p p p p o f p p p p p p p p p p p o o f p p p p p p p p p p p p p p o heck